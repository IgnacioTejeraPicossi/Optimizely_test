@model OptiDemoCms.Models.Blocks.HeroBlock

<div class="hero" data-testid="hero-block">
    <h1>@Model.Heading</h1>

    <div>
        @Html.PropertyFor(x => x.Text)
    </div>

    @if (Model.Image != null)
    {
        <img src="@Url.ContentUrl(Model.Image)" alt="@Model.Heading" />
    }
</div>